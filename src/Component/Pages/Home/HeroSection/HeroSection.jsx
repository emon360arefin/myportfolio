import React from 'react';

const HeroSection = () => {
    return (
        <div id='home' className='h-screen bg-blue-200'>
            <h2>Hero</h2>
        </div>
    );
};

export default HeroSection;